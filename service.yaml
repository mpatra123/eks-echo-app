apiVersion: v1
kind: Service
metadata:
  name: monica-echo-service
  namespace: shared-eks-activity
spec:
  selector:
    app: echo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678
